import AddProduct from "../(components)/addProduct";

const page = () => {
  return (
    <div className="text-white max-w-max mx-auto mt-4">
      <h1 className="text-2xl text-center mb-4">Add New Product</h1>
      <AddProduct />
    </div>
  );
};

export default page;
